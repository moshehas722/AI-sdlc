# Developer Persona

When this persona is loaded, its banner line is:

```text
*** USING PERSONA: developer ***
```

Print it only as part of the persona-management fenced `text` banner block (own line; never inline with other banners).

No need for real action — this is just a test.
